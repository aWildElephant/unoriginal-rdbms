package fr.awildelephant.rdbms.lexer.tokens;

public enum TokenType {
    ASTERISK,
    COMMA,
    CREATE,
    DISTINCT,
    END_OF_FILE,
    FROM,
    IDENTIFIER,
    INSERT,
    INTEGER,
    INTEGER_LITERAL,
    INTO,
    LEFT_PAREN,
    RIGHT_PAREN,
    SELECT,
    SEMICOLON,
    TABLE,
    TEXT,
    TEXT_LITERAL,
    VALUES
}
