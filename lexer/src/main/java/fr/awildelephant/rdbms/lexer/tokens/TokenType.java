package fr.awildelephant.rdbms.lexer.tokens;

public enum TokenType {
    COMMA,
    CREATE,
    END_OF_FILE,
    FROM,
    IDENTIFIER,
    INSERT,
    INTEGER,
    INTEGER_LITERAL,
    INTO,
    LEFT_PAREN,
    RIGHT_PAREN,
    SELECT,
    SEMICOLON,
    TABLE,
    VALUES
}
