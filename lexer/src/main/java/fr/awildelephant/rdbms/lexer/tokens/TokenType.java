package fr.awildelephant.rdbms.lexer.tokens;

public enum TokenType {
    AS,
    ASTERISK,
    CAST,
    CHAR,
    COMMA,
    COUNT,
    CREATE,
    DATE,
    DECIMAL,
    DECIMAL_LITERAL,
    DISTINCT,
    DROP,
    END_OF_FILE,
    FOREIGN,
    FROM,
    IDENTIFIER,
    INSERT,
    INTEGER,
    INTEGER_LITERAL,
    INTO,
    KEY,
    LEFT_PAREN,
    NOT,
    NULL,
    REFERENCES,
    RIGHT_PAREN,
    SELECT,
    SEMICOLON,
    TABLE,
    TEXT,
    TEXT_LITERAL,
    UNIQUE,
    VALUES,
    VARCHAR
}
