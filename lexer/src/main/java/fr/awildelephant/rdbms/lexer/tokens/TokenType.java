package fr.awildelephant.rdbms.lexer.tokens;

public enum TokenType {
    ASTERISK,
    COMMA,
    CREATE,
    DECIMAL,
    DECIMAL_LITERAL,
    DISTINCT,
    DROP,
    END_OF_FILE,
    FROM,
    IDENTIFIER,
    INSERT,
    INTEGER,
    INTEGER_LITERAL,
    INTO,
    LEFT_PAREN,
    NOT,
    NULL,
    RIGHT_PAREN,
    SELECT,
    SEMICOLON,
    TABLE,
    TEXT,
    TEXT_LITERAL,
    VALUES
}
