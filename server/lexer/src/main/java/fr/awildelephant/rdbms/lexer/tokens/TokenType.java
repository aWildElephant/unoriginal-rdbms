package fr.awildelephant.rdbms.lexer.tokens;

public enum TokenType {
    AS,
    ASTERISK,
    BY,
    CAST,
    CHAR,
    COMMA,
    COUNT,
    CREATE,
    DATE,
    DECIMAL,
    DECIMAL_LITERAL,
    DISTINCT,
    DROP,
    END_OF_FILE,
    FOREIGN,
    FROM,
    GROUP,
    IDENTIFIER,
    INSERT,
    INTEGER,
    INTEGER_LITERAL,
    INTO,
    KEY,
    LEFT_PAREN,
    MINUS,
    NOT,
    NULL,
    PLUS,
    REFERENCES,
    RIGHT_PAREN,
    SELECT,
    SEMICOLON,
    SOLIDUS,
    SUM,
    TABLE,
    TEXT,
    TEXT_LITERAL,
    UNIQUE,
    VALUES,
    VARCHAR
}
